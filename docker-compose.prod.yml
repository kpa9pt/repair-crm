services:
  gateway-blue:
    image: ${GATEWAY_BLUE_IMAGE}

  gateway-green:
    image: ${GATEWAY_GREEN_IMAGE}

  auth-blue:
    image: ${AUTH_BLUE_IMAGE}

  auth-green:
    image: ${AUTH_GREEN_IMAGE}

  nginx:
    image: ${NGINX_IMAGE}

  certbot:
    image: ${CERTBOT_IMAGE}

  migrations:
    image: ${MIGRATIONS_IMAGE}

  watchdog:
    image: ${WATCHDOG_IMAGE}