# service-registry.yaml
services:
  gateway:
    strategy: blue-green
    port: 8000
    healthcheck: /health
    paths:
      - 'services/gateway/**'
      - 'shared/**'
      - 'requirements.txt'

  auth:
    strategy: blue-green
    port: 8001
    healthcheck: /health
    paths:
      - 'services/auth/**'
      - 'shared/**'
      - 'requirements.txt'

  nginx:
    strategy: single
    paths:
      - 'services/nginx/**'

  certbot:
    strategy: single
    paths:
      - 'services/certbot/**'

  migrations:
    strategy: single
    paths:
      - 'services/migrations/**'
      - 'shared/**'
      - 'alembic.ini'
      - 'requirements.txt'

  watchdog:
    strategy: single
    paths:
      - 'services/watchdog/**'
      - 'scripts/rollback.py'

  telegram-bot:
    strategy: single
    paths:
      - 'services/telegram-bot/**'
      - 'shared/**'
      - 'requirements.txt'